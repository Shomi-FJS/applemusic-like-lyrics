# AMLL 远程网页控制技术实现文档

## 一、架构概述

AMLL (AppleMusic-Like-Lyrics) 项目实现了一套完整的远程网页控制系统，允许用户通过浏览器或移动设备远程控制播放器。该系统采用 **WebSocket + HTTP REST API** 混合架构，支持实时双向通信和标准 HTTP 请求。

### 1.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| 后端框架 | Tauri (Rust) + Axum |
| WebSocket | tokio-tungstenite |
| 前端框架 | React + TypeScript |
| 状态管理 | Jotai |
| 网易云集成 | BetterNCM 插件系统 |

### 1.2 核心架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         远程客户端                            │
│                  (浏览器 / 移动设备)                         │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP (13533) / WebSocket
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    Tauri 后端服务                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              HTTP Server (Axum)                       │  │
│  │  - REST API: /api/player/*                            │  │
│  │  - 静态文件服务: remote-index.html                     │  │
│  │  - WebSocket 升级: /api/player/events                │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          │                                  │
│  ┌───────────────────────▼──────────────────────────────┐  │
│  │         WebSocket Server (tokio-tungstenite)          │  │
│  │  - V1 二进制协议                                      │  │
│  │  - V2 混合协议 (JSON + Binary)                        │  │
│  │  - 连接管理 & 消息广播                                │  │
│  └───────────────────────┬──────────────────────────────┘  │
│                          │                                  │
│  ┌───────────────────────▼──────────────────────────────┐  │
│  │              Player Core (Rust)                      │  │
│  │  - 音频播放控制                                      │  │
│  │  - 歌词渲染                                          │  │
│  │  - 状态同步                                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              BetterNCM 插件 (网易云音乐)                      │
│  - 接收 AMLL 播放指令                                       │
│  - 调用网易云原生 API 播放歌曲                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、WebSocket 服务器实现

### 2.1 核心数据结构

```rust
use std::collections::HashMap;
use std::{net::SocketAddr, sync::Arc};
use tokio::sync::RwLock as TokioRwLock;
use tokio_tungstenite::tungstenite::Message;
use tokio_tungstenite::{WebSocketStream, accept_async};

type Connections = Arc<TokioRwLock<HashMap<SocketAddr, ConnectionInfo>>>;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum ProtocolType {
    Unknown,
    BinaryV1,
    HybridV2,
}

struct ConnectionInfo {
    sink: SplitSink<WebSocketStream<TcpStream>, Message>,
    protocol: ProtocolType,
}

pub struct AMLLWebSocketServer {
    app: AppHandle,
    server_handle: Option<JoinHandle<()>>,
    connections: Connections,
    channel: Option<Channel<v2::Payload>>,
    listen_addr: Option<String>,
}
```

### 2.2 服务器启动与监听

```rust
pub fn reopen(&mut self, addr: String, channel: Option<Channel<v2::Payload>>) {
    if let Some(task) = self.server_handle.take() {
        task.abort();
    }
    if addr.is_empty() {
        self.listen_addr = None;
        info!("WebSocket 服务器已关闭");
        return;
    }
    let app = self.app.clone();
    let connections = self.connections.clone();
    let channel_clone = channel.clone();
    self.channel = channel_clone;
    self.listen_addr = Some(addr.clone());

    self.server_handle = Some(tokio::spawn(async move {
        loop {
            info!("正在开启 WebSocket 服务器到 {addr}");
            match TcpListener::bind(&addr).await {
                Ok(listener) => {
                    info!("已开启 WebSocket 服务器到 {addr}");
                    while let Ok((stream, _)) = listener.accept().await {
                        tokio::spawn(Self::accept_conn(
                            stream,
                            app.clone(),
                            connections.clone(),
                            channel.clone(),
                        ));
                    }
                    warn!("WebSocket 监听器失效，正在尝试重启...");
                }
                Err(err) => {
                    error!("WebSocket 服务器 {addr} 开启失败: {err:?}");
                }
            }
            tokio::time::sleep(Duration::from_secs(1)).await;
        }
    }));
}
```

### 2.3 连接接受与协议识别

```rust
async fn accept_conn(
    stream: TcpStream,
    app: AppHandle,
    conns: Connections,
    channel: Option<Channel<v2::Payload>>,
) -> anyhow::Result<()> {
    let addr = stream.peer_addr()?;
    let addr_str = addr.to_string();
    info!("已接受套接字连接: {addr}");

    let wss = accept_async(stream).await?;
    info!("已连接 WebSocket 客户端: {addr}");
    app.emit("on-ws-protocol-client-connected", &addr_str)?;

    let (write_sink, mut read_stream) = wss.split();

    let mut temp_sink = Some(write_sink);

    if let Some(Ok(first_message)) = read_stream.next().await {
        let protocol_type = match first_message {
            Message::Text(ref text) => {
                if let Ok(v2_message) = serde_json::from_str::<v2::MessageV2>(text) {
                    if v2_message.payload == v2::Payload::Initialize {
                        info!("已识别为 HybridV2 协议");
                        ProtocolType::HybridV2
                    } else {
                        warn!("收到了一个非 Initialize 的 V2 消息，断开。");
                        return Ok(());
                    }
                } else {
                    warn!("发送了无法识别的文本消息，断开。");
                    return Ok(());
                }
            }
            Message::Binary(_) => {
                info!("已识别为 BinaryV1 协议");
                if let Some(channel) = channel.as_ref() {
                    if let Err(e) = Self::process_v1_message(first_message, channel).await {
                        error!("处理 V1 协议的消息时失败: {e:?}");
                        return Ok(());
                    }
                }
                ProtocolType::BinaryV1
            }
            _ => ProtocolType::Unknown,
        };

        if protocol_type != ProtocolType::Unknown
            && let Some(sink) = temp_sink.take()
        {
            conns.write().await.insert(
                addr,
                ConnectionInfo {
                    sink,
                    protocol: protocol_type,
                },
            );
        }
    }

    while let Some(Ok(message)) = read_stream.next().await {
        let conns_read = conns.read().await;
        if let Some(conn_info) = conns_read.get(&addr) {
            let process_result = match conn_info.protocol {
                ProtocolType::BinaryV1 => {
                    if let Some(channel) = channel.as_ref() {
                        Self::process_v1_message(message, channel).await
                    } else {
                        Ok(())
                    }
                }
                ProtocolType::HybridV2 => {
                    if let Some(channel) = channel.as_ref() {
                        Self::process_v2_message(message, channel).await
                    } else {
                        Ok(())
                    }
                }
                _ => Ok(()),
            };
            if let Err(e) = process_result {
                error!("处理消息失败: {e:?}");
                break;
            }
        }
    }

    info!("已断开 WebSocket 客户端: {addr}");
    app.emit("on-ws-protocol-client-disconnected", &addr_str)?;
    conns.write().await.remove(&addr);
    Ok(())
}
```

### 2.4 消息广播实现

```rust
pub async fn broadcast_payload(&mut self, payload: v2::Payload) {
    let mut conns = self.connections.write().await;

    let v2_msg = serde_json::to_string(&payload)
        .ok()
        .map(|s| Message::Text(s.into()));

    let v1_msg = if let Ok(v1_body) = v1::Body::try_from(payload.clone()) {
        v1::to_body(&v1_body)
            .ok()
            .map(|d| Message::Binary(d.into()))
    } else {
        None
    };

    let mut disconnected_addrs = Vec::new();

    for (addr, conn_info) in conns.iter_mut() {
        let msg_to_send = match conn_info.protocol {
            ProtocolType::BinaryV1 => v1_msg.as_ref(),
            ProtocolType::HybridV2 => v2_msg.as_ref(),
            _ => None,
        };

        if let Some(msg) = msg_to_send {
            if msg.is_empty() {
                continue;
            }
            if let Err(err) = conn_info.sink.send(msg.clone()).await {
                warn!("WebSocket 客户端 {addr} 发送失败: {err:?}");
                disconnected_addrs.push(*addr);
            }
        }
    }

    for addr in disconnected_addrs {
        conns.remove(&addr);
    }
}
```

---

## 三、HTTP REST API 实现

### 3.1 HTTP 服务器控制器

```rust
pub struct HttpServerController {
    app: AppHandle,
    ws_server: Arc<RwLock<AMLLWebSocketServer>>,
    server_handle: Option<JoinHandle<()>>,
    shutdown_tx: Option<oneshot::Sender<()>>,
}

impl HttpServerController {
    pub fn new(app: AppHandle, ws_server: Arc<RwLock<AMLLWebSocketServer>>) -> Self {
        Self {
            app,
            ws_server,
            server_handle: None,
            shutdown_tx: None,
        }
    }

    pub async fn set_enabled(&mut self, enabled: bool) {
        if enabled {
            self.start().await;
        } else {
            self.stop().await;
        }
    }

    async fn start(&mut self) {
        if self.server_handle.is_some() {
            return;
        }

        let Some(dist_dir) = find_dist_dir(&self.app) else {
            warn!("无法定位前端 dist 目录，HTTP 服务不会启动");
            return;
        };
        info!("HTTP 静态服务目录: {}", dist_dir.display());

        let addr: SocketAddr = ([0, 0, 0, 0], 13533).into();
        info!("HTTP 服务监听: http://{addr}/");

        let listener = match tokio::net::TcpListener::bind(addr).await {
            Ok(l) => l,
            Err(e) => {
                error!("HTTP 服务启动失败: {e:?}");
                return;
            }
        };

        let app = self.app.clone();
        let ws_server = self.ws_server.clone();
        let (shutdown_tx, shutdown_rx) = oneshot::channel();
        self.shutdown_tx = Some(shutdown_tx);
        self.server_handle = Some(tokio::spawn(async move {
            run_http_server(app, ws_server, dist_dir, listener, shutdown_rx).await;
        }));
    }

    async fn stop(&mut self) {
        if let Some(tx) = self.shutdown_tx.take() {
            let _ = tx.send(());
        }
        if let Some(handle) = self.server_handle.take() {
            handle.abort();
        }
    }
}
```

### 3.2 API 路由定义

```rust
pub fn build_router(
    app: AppHandle,
    ws_server: Arc<RwLock<AMLLWebSocketServer>>,
    dist_dir: PathBuf,
) -> Router {
    let (song_events, _) = broadcast::channel(64);
    let state = HttpServerState {
        app,
        ws_server,
        song_events,
    };
    Router::new()
        .route("/api/player/now-playing", get(api_now_playing))
        .route(
            "/api/player/play",
            post(|state: State<HttpServerState>| async move {
                api_player_action(state, "play").await
            }),
        )
        .route(
            "/api/player/pause",
            post(|state: State<HttpServerState>| async move {
                api_player_action(state, "pause").await
            }),
        )
        .route(
            "/api/player/next",
            post(|state: State<HttpServerState>| async move {
                api_player_action(state, "next").await
            }),
        )
        .route(
            "/api/player/prev",
            post(|state: State<HttpServerState>| async move {
                api_player_action(state, "prev").await
            }),
        )
        .route("/api/player/fullscreen", post(api_fullscreen))
        .route("/api/player/command", post(api_player_command))
        .route("/api/player/always-on-top", post(api_always_on_top))
        .route("/api/ws/listen-addr", post(api_ws_listen_addr))
        .route("/api/player/song/:id", post(api_play_song))
        .route("/api/player/expand-url", get(api_expand_url))
        .route("/api/player/events", get(api_player_events_ws))
        .route(
            "/",
            get_service(ServeFile::new(dist_dir.join("remote-index.html"))),
        )
        .fallback_service(ServeDir::new(dist_dir).append_index_html_on_directories(false))
        .layer(CorsLayer::permissive())
        .with_state(state)
}
```

### 3.3 获取当前播放信息

```rust
#[derive(Serialize)]
#[serde(rename_all = "camelCase")]
struct NowPlayingResponse {
    title: String,
    artist: String,
    album: String,
    is_playing: bool,
    always_on_top: bool,
    cover: Option<String>,
}

async fn api_now_playing(State(state): State<HttpServerState>) -> Response {
    if let Some(info) = REMOTE_NOW_PLAYING.read().await.clone() {
        let always_on_top = state
            .app
            .get_webview_window("main")
            .and_then(|w| w.is_always_on_top().ok())
            .unwrap_or(false);
        return Json(NowPlayingResponse {
            title: info.title,
            artist: info.artist,
            album: info.album,
            is_playing: info.is_playing,
            always_on_top,
            cover: info.cover,
        })
        .into_response();
    }

    let snapshot = NOW_PLAYING.read().ok().and_then(|guard| guard.clone());
    let Some(snapshot) = snapshot else {
        return StatusCode::NO_CONTENT.into_response();
    };

    if snapshot.title.is_empty() && snapshot.artist.is_empty() && snapshot.album.is_empty() {
        return StatusCode::NO_CONTENT.into_response();
    }

    let always_on_top = state
        .app
        .get_webview_window("main")
        .and_then(|w| w.is_always_on_top().ok())
        .unwrap_or(false);

    Json(NowPlayingResponse {
        title: snapshot.title,
        artist: snapshot.artist,
        album: snapshot.album,
        is_playing: snapshot.is_playing,
        always_on_top,
        cover: snapshot.cover_data_url,
    })
    .into_response()
}
```

### 3.4 播放器控制命令

```rust
#[derive(serde::Serialize, serde::Deserialize, Clone)]
#[serde(tag = "command", rename_all = "camelCase")]
enum RemoteCommand {
    Pause,
    Resume,
    ForwardSong,
    BackwardSong,
    SetVolume { volume: f64 },
    SeekPlayProgress { progress: f64 },
}

async fn api_player_command(
    State(state): State<HttpServerState>,
    Json(cmd): Json<RemoteCommand>,
) -> StatusCode {
    emit_remote_command(&state, &cmd);
    let ok = match cmd {
        RemoteCommand::Pause => {
            send_player_command(amll_player_core::AudioThreadMessage::PauseAudio).await
        }
        RemoteCommand::Resume => {
            send_player_command(amll_player_core::AudioThreadMessage::ResumeAudio).await
        }
        RemoteCommand::ForwardSong => {
            send_player_command(amll_player_core::AudioThreadMessage::NextSong).await
        }
        RemoteCommand::BackwardSong => {
            send_player_command(amll_player_core::AudioThreadMessage::PrevSong).await
        }
        RemoteCommand::SetVolume { volume } => {
            let v = volume.clamp(0.0, 1.0);
            send_player_command(amll_player_core::AudioThreadMessage::SetVolume { volume: v }).await
        }
        RemoteCommand::SeekPlayProgress { progress } => {
            let position = progress.max(0.0);
            send_player_command(amll_player_core::AudioThreadMessage::SeekAudio { position }).await
        }
    };
    if ok {
        StatusCode::OK
    } else {
        StatusCode::INTERNAL_SERVER_ERROR
    }
}
```

### 3.5 播放指定歌曲

```rust
#[derive(Deserialize, Serialize, Clone)]
#[serde(rename_all = "camelCase")]
struct PlaySongEvent {
    id: String,
    source: String,
}

async fn api_play_song(
    State(state): State<HttpServerState>,
    axum::extract::Path(id): axum::extract::Path<String>,
) -> StatusCode {
    let event = PlaySongEvent {
        id,
        source: "ncm".to_string(),
    };
    let _ = state.song_events.send(event.clone());
    if let Err(err) = state.app.emit("remote-play-song", &event) {
        error!("Failed to emit remote-play-song: {:?}", err);
        return StatusCode::INTERNAL_SERVER_ERROR;
    }
    StatusCode::OK
}
```

### 3.6 播放事件 WebSocket 推送

```rust
async fn api_player_events_ws(
    ws: WebSocketUpgrade,
    State(state): State<HttpServerState>,
) -> Response {
    ws.on_upgrade(move |socket| handle_player_events_socket(socket, state.song_events.subscribe()))
        .into_response()
}

async fn handle_player_events_socket(
    mut socket: WebSocket,
    mut rx: broadcast::Receiver<PlaySongEvent>,
) {
    loop {
        tokio::select! {
            msg = rx.recv() => {
                let event = match msg {
                    Ok(event) => event,
                    Err(broadcast::error::RecvError::Lagged(_)) => continue,
                    Err(_) => break,
                };
                let Ok(text) = serde_json::to_string(&event) else {
                    continue;
                };
                if socket.send(Message::Text(text)).await.is_err() {
                    break;
                }
            }
            inbound = socket.recv() => {
                match inbound {
                    Some(Ok(Message::Close(_))) | None => break,
                    Some(Ok(Message::Ping(v))) => {
                        if socket.send(Message::Pong(v)).await.is_err() {
                            break;
                        }
                    }
                    Some(Ok(_)) => {}
                    Some(Err(_)) => break,
                }
            }
        }
    }
}
```

---

## 四、前端远程控制界面

### 4.1 界面架构

远程控制界面是一个独立的单页应用，通过 HTTP API 与后端通信，并使用 WebSocket 接收实时事件。

### 4.2 核心 JavaScript 逻辑 (remote-index.html)

```javascript
const DEFAULT_PORT = 13533;
const API_BASE = `http://${window.location.host}/api/player`;

let ws = null;
let reconnectTimer = null;
let currentSong = null;

function connectWebSocket() {
    const wsUrl = `ws://${window.location.host}/api/player/events`;
    
    ws = new WebSocket(wsUrl);
    
    ws.onopen = () => {
        console.log('WebSocket connected');
        updateConnectionStatus(true);
    };
    
    ws.onmessage = (event) => {
        const data = JSON.parse(event.data);
        handleSongEvent(data);
    };
    
    ws.onclose = () => {
        console.log('WebSocket disconnected');
        updateConnectionStatus(false);
        scheduleReconnect();
    };
    
    ws.onerror = (error) => {
        console.error('WebSocket error:', error);
    };
}

function scheduleReconnect() {
    if (reconnectTimer) return;
    reconnectTimer = setTimeout(() => {
        reconnectTimer = null;
        connectWebSocket();
    }, 2000);
}

function handleSongEvent(event) {
    if (event.source === 'ncm' && event.id) {
        currentSong = event;
        updateUI();
    }
}

async function fetchNowPlaying() {
    try {
        const response = await fetch(`${API_BASE}/now-playing`);
        if (response.ok) {
            const data = await response.json();
            updateNowPlaying(data);
        }
    } catch (error) {
        console.error('Failed to fetch now playing:', error);
    }
}

async function sendPlayerCommand(command) {
    try {
        const response = await fetch(`${API_BASE}/command`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(command),
        });
        return response.ok;
    } catch (error) {
        console.error('Failed to send command:', error);
        return false;
    }
}

function play() {
    sendPlayerCommand({ command: 'resume' });
}

function pause() {
    sendPlayerCommand({ command: 'pause' });
}

function next() {
    sendPlayerCommand({ command: 'forwardSong' });
}

function prev() {
    sendPlayerCommand({ command: 'backwardSong' });
}

function setVolume(volume) {
    sendPlayerCommand({ command: 'setVolume', volume });
}

function seek(progress) {
    sendPlayerCommand({ command: 'seekPlayProgress', progress });
}

function updateConnectionStatus(connected) {
    const statusDot = document.querySelector('.status-dot');
    if (connected) {
        statusDot.classList.add('connected');
        statusDot.textContent = '已连接';
    } else {
        statusDot.classList.remove('connected');
        statusDot.textContent = '未连接';
    }
}

function updateNowPlaying(data) {
    document.querySelector('.song-title').textContent = data.title;
    document.querySelector('.song-artist').textContent = data.artist;
    document.querySelector('.song-album').textContent = data.album;
    
    const coverEl = document.querySelector('.song-cover');
    if (data.cover) {
        coverEl.src = data.cover;
        coverEl.style.display = 'block';
    } else {
        coverEl.style.display = 'none';
    }
    
    updatePlayPauseButton(data.is_playing);
}

function updatePlayPauseButton(isPlaying) {
    const btn = document.querySelector('.play-pause-btn');
    if (isPlaying) {
        btn.innerHTML = '<svg>...</svg>';
        btn.onclick = pause;
    } else {
        btn.innerHTML = '<svg>...</svg>';
        btn.onclick = play;
    }
}

function updateUI() {
    if (currentSong) {
        document.querySelector('.current-song-id').textContent = currentSong.id;
    }
}

connectWebSocket();
setInterval(fetchNowPlaying, 5000);
```

### 4.3 UI 样式系统

界面采用现代玻璃拟态设计，支持深色/浅色主题切换。

```css
:root {
    --transition-speed: 0.3s;
    --transition-easing: cubic-bezier(0.4, 0, 0.2, 1);
}

[data-theme="dark"] {
    --bg-color: #050509;
    --card-bg: rgba(9, 9, 17, 0.82);
    --card-border: rgba(255, 255, 255, 0.08);
    --accent-primary: #6366f1;
    --accent-secondary: #ec4899;
    --text-main: #ffffff;
    --text-sub: #a5b4fc;
}

[data-theme="light"] {
    --bg-color: #f8fafc;
    --card-bg: rgba(255, 255, 255, 0.9);
    --card-border: rgba(148, 163, 184, 0.45);
    --accent-primary: #4f46e5;
    --accent-secondary: #db2777;
    --text-main: #020617;
    --text-sub: #475569;
}

.shell {
    background: var(--glass-panel-bg);
    backdrop-filter: blur(34px) saturate(1.2);
    -webkit-backdrop-filter: blur(34px) saturate(1.2);
    border: 1px solid var(--glass-panel-border);
    box-shadow: var(--glass-panel-shadow);
}

.control-btn {
    background: var(--glass-button-bg);
    border: 1px solid var(--glass-button-border);
    backdrop-filter: blur(16px) saturate(1.2);
    transition: all var(--transition-speed) var(--transition-easing);
}

.control-btn:hover {
    background: var(--btn-glass-hover);
    transform: scale(1.05);
}
```

---

## 五、网易云音乐集成 (BetterNCM 插件)

### 5.1 插件核心实现

```javascript
plugin.onLoad(function () {
    const DEFAULT_PORT = 13533;
    const DEFAULT_ENABLED = true;

    let config = {
        port: Number(plugin.getConfig("amllPort", DEFAULT_PORT)) || DEFAULT_PORT,
        enabled: plugin.getConfig("amllEnabled", "true") === "true",
    };

    let ws = null;
    let reconnectHandle = null;
    let lastPlayed = { id: null, ts: 0 };

    function getWsUrl() {
        return `ws://127.0.0.1:${config.port}/api/player/events`;
    }

    function saveConfig() {
        plugin.setConfig("amllPort", config.port);
        plugin.setConfig("amllEnabled", String(config.enabled));
    }

    function toast(message) {
        try {
            if (betterncm?.utils?.toast) betterncm.utils.toast(message);
        } catch (_) {}
    }

    function scheduleReconnect() {
        if (reconnectHandle) return;
        reconnectHandle = setTimeout(() => {
            reconnectHandle = null;
            connect();
        }, 1000);
    }

    function disconnect() {
        if (reconnectHandle) {
            clearTimeout(reconnectHandle);
            reconnectHandle = null;
        }
        try {
            if (ws) ws.close();
        } catch (_) {}
        ws = null;
    }

    function playSongById(id) {
        try {
            const fn = betterncm.ncm.findNativeFunction(ctl.actionManager, [
                ".logSource=",
                ".action",
            ]);
            ctl.actionManager[fn]({
                id: String(id),
                type: "4",
                action: "play",
                from: 0,
                href: "",
                data: {},
            });
        } catch (e) {
            console.error("[AMLLRemoteSong] playSongById failed:", e);
        }
    }

    function handleSongEvent(evt) {
        if (!evt || evt.source !== "ncm" || !evt.id) return;

        const now = Date.now();
        if (lastPlayed.id === evt.id && now - lastPlayed.ts < 2000) return;
        lastPlayed = { id: evt.id, ts: now };

        playSongById(evt.id);
    }

    function connect() {
        disconnect();
        if (!config.enabled) return;

        const url = getWsUrl();
        try {
            ws = new WebSocket(url);
        } catch (e) {
            console.error("[AMLLRemoteSong] WebSocket create failed:", e);
            scheduleReconnect();
            return;
        }

        ws.addEventListener("open", () => {
            toast(`AMLLRemoteSong 已连接: ${url}`);
        });

        ws.addEventListener("message", (e) => {
            try {
                const evt = JSON.parse(e.data);
                handleSongEvent(evt);
            } catch (_) {}
        });

        ws.addEventListener("close", () => {
            scheduleReconnect();
        });

        ws.addEventListener("error", () => {
            scheduleReconnect();
        });
    }

    connect();

    plugin.onConfig(function (tools) {
        const enabledCheckbox = dom("input", {
            type: "checkbox",
            checked: config.enabled,
            onchange: (e) => {
                config.enabled = e.target.checked;
                saveConfig();
                if (config.enabled) connect();
                else disconnect();
            },
        });

        const portInput = tools.makeInput(String(config.port), {
            onchange: (e) => {
                const val = parseInt(e.target.value);
                if (val > 0 && val < 65536) {
                    config.port = val;
                    saveConfig();
                    if (config.enabled) connect();
                }
            },
        });

        const testBtn = tools.makeBtn("测试连接", () => {
            const url = getWsUrl();
            const testWs = new WebSocket(url);
            testWs.onopen = () => {
                toast("测试连接成功！");
                testWs.close();
            };
            testWs.onerror = () => {
                toast("测试连接失败，请检查端口或 AMLL 是否启动");
            };
        });

        const ncmIdInput = tools.makeInput("");
        ncmIdInput.placeholder = "输入网易云音乐 ID";
        const playTestBtn = tools.makeBtn("测试播放", () => {
            const id = ncmIdInput.value.trim();
            if (id) {
                playSongById(id);
                toast(`尝试播放 ID: ${id}`);
            } else {
                toast("请输入有效的 NCM ID");
            }
        });

        return dom(
            "div",
            { style: "display: flex; flex-direction: column; gap: 10px;" },
            dom(
                "div",
                { style: "display: flex; align-items: center; gap: 10px;" },
                dom("span", { innerText: "启用插件" }),
                enabledCheckbox,
            ),
            dom(
                "div",
                { style: "display: flex; flex-direction: column; gap: 5px;" },
                dom("span", { innerText: "AMLL 端口 (默认 13533)" }),
                portInput,
            ),
            dom("div", {}, testBtn),
            dom(
                "div",
                {
                    innerText: "修改设置后会自动重连",
                    style: "color: #888; font-size: 0.8em;",
                },
            ),
            dom(
                "div",
                { style: "margin-top: 10px; border-top: 1px solid #ccc; padding-top: 10px; display: flex; flex-direction: column; gap: 5px;" },
                dom("span", { innerText: "播放测试 (NCM ID)" }),
                dom(
                    "div",
                    { style: "display: flex; gap: 10px;" },
                    ncmIdInput,
                    playTestBtn
                )
            )
        );
    });
});
```

### 5.2 插件配置文件 (manifest.json)

```json
{
    "manifest_version": 2,
    "name": "AMLLRemoteSong",
    "version": "1.0.0",
    "author": "AMLL Team",
    "description": "AMLL 远程点歌插件 - 通过 WebSocket 接收 AMLL 的播放指令并在网易云音乐中播放",
    "main": "main.js",
    "injects": {},
    "load_after": []
}
```

---

## 六、状态管理 (Jotai)

### 6.1 应用状态定义

```typescript
import { invoke } from "@tauri-apps/api/core";
import { atom } from "jotai";
import { atomWithStorage } from "jotai/utils";

export enum MusicContextMode {
    Local = "local",
    WSProtocol = "ws-protocol",
    SystemListener = "system-listener",
}

export const musicContextModeAtom = atomWithStorage(
    "amll-player.musicContextMode",
    MusicContextMode.Local,
);

const enableHttpServerInternalAtom = atomWithStorage(
    "amll-player.enableHttpServer",
    true,
);

export const enableHttpServerAtom = atom(
    (get) => get(enableHttpServerInternalAtom),
    (_get, set, enabled: boolean) => {
        set(enableHttpServerInternalAtom, enabled);
        invoke("set_http_server_enabled", { enabled }).catch((err) => {
            console.error("设置 13533 端口控制服务状态失败", err);
        });
    },
);

export const wsProtocolListenAddrAtom = atomWithStorage(
    "amll-player.wsProtocolListenAddr",
    "localhost:11444",
);

export const wsProtocolConnectedAddrsAtom = atom(new Set<string>());
```

### 6.2 WebSocket 接收页面

```typescript
import { useAtom, useAtomValue, useSetAtom } from "jotai";
import { type FC, useEffect, useMemo } from "react";
import {
    wsProtocolListenAddrAtom,
    wsProtocolConnectedAddrsAtom,
    musicContextModeAtom,
    MusicContextMode,
} from "../../states/appAtoms";

export const Component: FC = () => {
    const [wsProtocolListenAddr, setWsProtocolListenAddr] = useAtom(
        wsProtocolListenAddrAtom,
    );
    const connectedAddrs = useAtomValue(wsProtocolConnectedAddrsAtom);
    const setMusicContextMode = useSetAtom(musicContextModeAtom);
    const connectedAddrsArray = useMemo(
        () => [...connectedAddrs],
        [connectedAddrs],
    );

    useEffect(() => {
        setMusicContextMode(MusicContextMode.WSProtocol);
        return () => {
            setMusicContextMode(MusicContextMode.Local);
        };
    }, [setMusicContextMode]);

    return (
        <Container mx={{ initial: "4", sm: "9" }} pt="env(safe-area-inset-top)" mb="150px">
            <Card mt="2">
                <Flex align="center">
                    <Box flexGrow="1">
                        <Text>开放网络地址</Text>
                    </Box>
                    <TextField.Root
                        type="url"
                        placeholder="0.0.0.0:11444"
                        value={wsProtocolListenAddr}
                        onChange={(e) => setWsProtocolListenAddr(e.target.value)}
                    />
                </Flex>
            </Card>
            <Card mt="2">
                <Text weight="bold">已连接的客户端</Text>
                {connectedAddrsArray.map((addr) => (
                    <Box key={`connected-addr-${addr}`}>
                        <Text color="gray">{addr}</Text>
                    </Box>
                ))}
                {connectedAddrsArray.length === 0 && (
                    <Box>
                        <Text color="gray">无</Text>
                    </Box>
                )}
            </Card>
        </Container>
    );
};
```

---

## 七、API 接口文档

### 7.1 REST API

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/player/now-playing` | 获取当前播放信息 |
| POST | `/api/player/play` | 播放 |
| POST | `/api/player/pause` | 暂停 |
| POST | `/api/player/next` | 下一首 |
| POST | `/api/player/prev` | 上一首 |
| POST | `/api/player/command` | 通用命令接口 |
| POST | `/api/player/fullscreen` | 切换全屏 |
| POST | `/api/player/always-on-top` | 设置窗口置顶 |
| POST | `/api/player/song/:id` | 播放指定 ID 歌曲 |
| GET | `/api/player/expand-url` | 展开短链接 |
| GET | `/api/player/events` | WebSocket 升级端点 |
| POST | `/api/ws/listen-addr` | 设置 WebSocket 监听地址 |

### 7.2 WebSocket 协议

#### V1 二进制协议
- 客户端发送二进制消息
- 服务器解析后转换为 V2 Payload

#### V2 混合协议
- 初始化握手：发送 JSON `{"payload":"Initialize"}`
- 后续消息支持 JSON 或二进制格式

---

## 八、部署与使用

### 8.1 启动服务

```bash
cd packages/player
npm install
npm run tauri dev
```

### 8.2 访问远程控制界面

服务启动后，在浏览器中访问：
```
http://localhost:13533
```

### 8.3 安装 BetterNCM 插件

1. 安装 BetterNCM 到网易云音乐
2. 将 `AMLLRemoteSong.plugin` 文件夹复制到 BetterNCM 插件目录
3. 在 BetterNCM 设置中启用插件
4. 配置端口（默认 13533）

### 8.4 网络访问配置

如需从局域网其他设备访问：
- 将 HTTP 服务器监听地址设置为 `0.0.0.0:13533`
- 将 WebSocket 监听地址设置为 `0.0.0.0:11444`
- 确保防火墙允许相应端口

---

## 九、技术亮点

### 9.1 双协议支持
- 同时支持 V1 二进制协议和 V2 混合协议
- 自动识别客户端协议类型
- 向后兼容旧版客户端

### 9.2 自动重连机制
- WebSocket 连接断开后自动重连
- BetterNCM 插件具备连接恢复能力
- 防抖机制避免重复操作

### 9.3 实时事件推送
- 使用 broadcast channel 实现多客户端事件分发
- 播放歌曲事件实时推送给所有连接的客户端
- 支持连接状态监控

### 9.4 玻璃拟态 UI
- 现代化设计语言
- 深色/浅色主题切换
- 响应式布局，支持移动设备

### 9.5 跨平台集成
- Tauri 桌面应用
- BetterNCM 网易云插件
- 浏览器远程控制
- 移动设备访问

---

## 十一、FFT 系统音频检测技术实现

AMLL 项目实现了完整的 FFT (快速傅里叶变换) 音频频谱分析系统，用于实时可视化音频波形。该系统采用 WebAssembly 技术在浏览器端进行高性能音频处理。

### 11.1 技术架构

| 层级 | 技术选型 |
|------|----------|
| 音频处理 | Rust + WebAssembly |
| FFT 计算 | spectrum-analyzer |
| 重采样 | rubato |
| 前端绑定 | wasm-bindgen |
| 可视化 | HTML5 Canvas + React |

### 11.2 FFT 核心数据结构

```rust
use instant::Instant;
use std::{cell::Cell, collections::VecDeque};
use spectrum_analyzer::*;

#[wasm_bindgen]
pub struct FFTPlayer {
    last_fft_time: Instant,
    rate: usize,
    channels: usize,
    result_buf: [f32; 2048],
    pcm_queue: VecDeque<f32>,
    fft_duration: usize,
    resampler: Option<FastFixedOutResampler<f32>>,
    freq_range: Cell<(f32, f32)>,
}
```

**核心字段说明：**
- `last_fft_time`: 上次 FFT 计算时间，用于控制采样队列长度
- `result_buf`: FFT 结果缓冲区，存储 2048 个频率点的幅值
- `pcm_queue`: PCM 音频数据队列，用于累积待处理的音频样本
- `resampler`: 音频重采样器，将不同采样率的音频统一到 44100Hz
- `freq_range`: 频率范围，默认 80Hz - 2000Hz

### 11.3 音频重采样实现

```rust
use symphonia_core::conv::{FromSample, IntoSample};
use symphonia_core::sample::Sample;

pub type FastFixedOutResampler<T> = Resampler<T, rubato::FastFixedOut<f32>>;

pub struct Resampler<T, S> {
    resampler: S,
    input: Vec<Vec<f32>>,
    output: Vec<Vec<f32>>,
    interleaved: Vec<T>,
    target_channels: usize,
    frames_len: usize,
}

impl<T> Resampler<T, rubato::FastFixedOut<f32>>
where
    T: Sample + FromSample<f32> + IntoSample<f32> + Send,
{
    pub fn new_fast_fixed(
        src_channels: usize,
        src_rate: usize,
        to_sample_rate: usize,
        to_channels: usize,
        duration: u64,
    ) -> Self {
        let resample_ratio = to_sample_rate as f64 / src_rate as f64;

        let resampler = rubato::FastFixedOut::new(
            resample_ratio,
            2.0,
            rubato::PolynomialDegree::Nearest,
            duration as usize,
            src_channels,
        ).unwrap();

        Self::new_inner(resampler, to_channels)
    }
}
```

**重采样特性：**
- 支持任意采样率转换（如 48000Hz → 44100Hz）
- 支持多声道到单声道的转换
- 使用 rubato 库的高质量插值算法

### 11.4 FFT 计算核心实现

```rust
impl FFTPlayer {
    pub fn read(&mut self, buf: &mut [f32]) -> bool {
        if self.pcm_queue.len() < 2048 {
            self.last_fft_time = Instant::now();
            return false;
        }

        let (start_freq, end_freq) = self.freq_range.get();

        let mut fft_buf = [0.0; 2048];
        self.pcm_queue
            .iter()
            .take(2048)
            .enumerate()
            .for_each(|(i, v)| {
                fft_buf[i] = *v;
            });

        let fft_buf = windows::hamming_window(&fft_buf);

        match samples_fft_to_spectrum(
            &fft_buf,
            44100,
            FrequencyLimit::Range(start_freq, end_freq),
            Some(&scaling::divide_by_N_sqrt),
        ) {
            Ok(spec) => {
                let result_buf_len = self.result_buf.len() as f32;
                let freq_min = spec.min_fr().val();
                let freq_max = spec.max_fr().val();
                let freq_range = freq_max - freq_min;
                self.result_buf.iter_mut().enumerate().for_each(|(i, v)| {
                    let freq = i as f32 / result_buf_len * freq_range + freq_min;
                    let freq = freq.clamp(freq_min, freq_max);
                    *v += spec.freq_val_exact(freq).val();
                    *v /= 2.0;
                });
                vec_interp(&self.result_buf, buf);

                let elapsed = self.last_fft_time.elapsed();
                let elapsed_sec = elapsed.as_secs_f64();
                self.last_fft_time = Instant::now();

                let cut_len = (elapsed_sec * 44100.0) as usize;
                for _ in 0..cut_len {
                    self.pcm_queue.pop_front();
                }
                self.pcm_queue.truncate(2048 * 4);
                true
            }
            Err(e) => {
                eprintln!("FFT error: {:?}", e);
                false
            }
        }
    }
}
```

**FFT 计算流程：**
1. 从 PCM 队列中提取 2048 个样本
2. 应用汉明窗（Hamming Window）减少频谱泄漏
3. 使用 `samples_fft_to_spectrum` 进行 FFT 变换
4. 限制频率范围（80Hz - 2000Hz）
5. 应用 `divide_by_N_sqrt` 缩放因子
6. 线性插值到目标缓冲区
7. 根据时间消耗移除已处理的样本

### 11.5 音频数据输入

```rust
impl FFTPlayer {
    pub fn push_data<T: Sample + FromSample<f32> + IntoSample<f32> + Send>(
        &mut self,
        rate: usize,
        channels: usize,
        decoded: &[T],
    ) {
        if decoded.is_empty() {
            return;
        }

        let should_reset_fft = self.resampler.is_none()
            || self.fft_duration < decoded.len()
            || self.channels != channels
            || self.rate != rate;

        if should_reset_fft {
            self.fft_duration = self.fft_duration.max(decoded.len());
            self.channels = channels;
            self.rate = rate;

            let resampler = FastFixedOutResampler::new_fast_fixed(
                channels,
                rate,
                44100,
                1,
                self.fft_duration as _,
            );

            self.resampler = Some(resampler);
            self.pcm_queue.clear();
        }

        let rsp = self.resampler.as_mut().unwrap();
        rsp.resample(channels, decoded);

        while let Some(buf) = rsp.flush() {
            for v in buf.iter() {
                self.pcm_queue.push_back(*v);
            }
        }
    }
}
```

**数据输入特性：**
- 支持多种音频格式（i8, u8, i16, u16, f32, f64）
- 自动检测采样率变化并重置重采样器
- 支持多声道音频自动混合为单声道

### 11.6 WebAssembly 绑定

```rust
#[wasm_bindgen]
impl FFTPlayer {
    #[wasm_bindgen(constructor)]
    pub fn new_js() -> Self {
        Self::new()
    }

    #[wasm_bindgen(js_name = "hasData")]
    pub fn has_data_js(&self) -> bool {
        self.has_data()
    }

    #[wasm_bindgen(js_name = "pushDataI8")]
    pub fn push_data_i8_js(&mut self, rate: usize, channels: usize, decoded: &[i8]) {
        self.push_data(rate, channels, decoded);
    }

    #[wasm_bindgen(js_name = "pushDataI16")]
    pub fn push_data_i16_js(&mut self, rate: usize, channels: usize, decoded: &[i16]) {
        self.push_data(rate, channels, decoded);
    }

    #[wasm_bindgen(js_name = "pushDataF32")]
    pub fn push_data_f32_js(&mut self, rate: usize, channels: usize, decoded: &[f32]) {
        self.push_data(rate, channels, decoded);
    }

    #[wasm_bindgen(js_name = "setFreqRange")]
    pub fn set_freq_range_js(&self, start_freq: f32, end_freq: f32) {
        self.set_freq_range(start_freq, end_freq);
    }

    #[wasm_bindgen(js_name = "read")]
    pub fn read_js(&mut self, buf: &mut [f32]) -> bool {
        self.read(buf)
    }
}
```

### 11.7 React 前端集成

#### 11.7.1 系统音频监听模式

```typescript
import { FFTPlayer } from "@applemusic-like-lyrics/fft";
import { fftDataAtom, fftDataRangeAtom } from "@applemusic-like-lyrics/react-full";
import { useAtomValue, useSetAtom, useStore } from "jotai";
import { type FC, useEffect, useRef } from "react";
import { listen } from "@tauri-apps/api/event";

export const SystemListenerMusicContext: FC = () => {
    const store = useStore();
    const fftPlayer = useRef<FFTPlayer | undefined>(undefined);
    const fftDataRange = useAtomValue(fftDataRangeAtom);

    useEffect(() => {
        const fft = new FFTPlayer();
        fft.setFreqRange(fftDataRange[0], fftDataRange[1]);
        fftPlayer.current = fft;
        const result = new Float32Array(64);
        let animationFrameId: number;

        const onFFTFrame = () => {
            fftPlayer.current?.read(result);
            store.set(fftDataAtom, [...result]);
            animationFrameId = requestAnimationFrame(onFFTFrame);
        };
        animationFrameId = requestAnimationFrame(onFFTFrame);

        return () => {
            cancelAnimationFrame(animationFrameId);
            fftPlayer.current?.free();
            fftPlayer.current = undefined;
        };
    }, [fftDataRange, store]);

    useEffect(() => {
        const unlisten = listen<number[]>("smtc_update", (event) => {
            if (event.payload.type === "audioData") {
                fftPlayer.current?.pushDataF32(44100, 2, event.payload.data);
            }
        });
        return () => { unlisten.then(fn => fn()); };
    }, []);

    return null;
};
```

#### 11.7.2 WebSocket 协议模式

```typescript
export const WSProtocolMusicContext: FC<WSProtocolMusicContextProps> = ({
    isLyricOnly = false,
}) => {
    const store = useStore();
    const fftPlayer = useRef<FFTPlayer | undefined>(undefined);
    const fftDataRange = useAtomValue(fftDataRangeAtom);

    useEffect(() => {
        let canceled = false;
        const fft = new FFTPlayer();
        fft.setFreqRange(fftDataRange[0], fftDataRange[1]);
        fftPlayer.current = fft;
        const result = new Float32Array(64);

        const onFFTFrame = () => {
            if (canceled) return;
            fftPlayer.current?.read(result);
            store.set(fftDataAtom, [...result]);
            requestAnimationFrame(onFFTFrame);
        };

        requestAnimationFrame(onFFTFrame);

        return () => {
            canceled = true;
            fftPlayer.current?.free();
            fftPlayer.current = undefined;
        };
    }, [fftDataRange, store]);

    const handleAudioData = (data: number[]) => {
        fftPlayer.current?.pushDataF32(44100, 2, data);
    };

    return null;
};
```

### 11.8 音频可视化组件

```typescript
import {
    type FC,
    type HTMLProps,
    useEffect,
    useLayoutEffect,
    useRef,
} from "react";

export const AudioFFTVisualizer: FC<
    {
        fftData: number[];
    } & HTMLProps<HTMLCanvasElement>
> = ({ fftData, ...props }) => {
    const canvasRef = useRef<HTMLCanvasElement>(null);
    const fftDataRef = useRef<number[]>(undefined);
    if (fftDataRef.current === undefined) {
        fftDataRef.current = fftData;
    }

    useEffect(() => {
        fftDataRef.current = fftData;
    }, [fftData]);

    useLayoutEffect(() => {
        const canvas = canvasRef.current;

        if (canvas) {
            const ctx = canvas.getContext("2d");
            if (ctx) {
                let targetSize = { width: 0, height: 0 };
                const obs = new ResizeObserver((sizes) => {
                    for (const size of sizes) {
                        targetSize = {
                            width: size.contentRect.width * window.devicePixelRatio,
                            height: size.contentRect.height * window.devicePixelRatio,
                        };
                    }
                });

                obs.observe(canvas);

                let maxValue = 100;
                let stopped = false;
                let buf: number[] = [];

                function resampleLinear(src: number[], dstLen: number): number[] {
                    const n = src.length;
                    if (dstLen <= 0 || n === 0) return [];
                    if (n === dstLen) return src.slice();
                    const out = new Array(dstLen);
                    const scale = (n - 1) / Math.max(1, dstLen - 1);
                    for (let i = 0; i < dstLen; i++) {
                        const x = i * scale;
                        const x0 = Math.floor(x);
                        const x1 = Math.min(n - 1, x0 + 1);
                        const t = x - x0;
                        out[i] = src[x0] * (1 - t) + src[x1] * t;
                    }
                    return out;
                }

                function onFrame() {
                    if (!(canvas && ctx) || stopped) return;
                    const width = canvas.width;
                    const height = canvas.height;
                    if (targetSize.width !== width || targetSize.height !== height) {
                        canvas.width = targetSize.width;
                        canvas.height = targetSize.height;
                    }

                    const processed = fftDataRef.current ?? [];
                    if (buf.length !== processed.length) {
                        buf = [...processed];
                    } else {
                        for (let i = 0; i < buf.length; i++) {
                            let t = processed[i];
                            t = t * Math.min(((i + 5) / buf.length) * 4, 1);
                            buf[i] += t * 2;
                            buf[i] /= 3;
                        }
                    }

                    ctx.clearRect(0, 0, width, height);

                    const dpr = window.devicePixelRatio || 1;
                    const desiredSpacing = 8 * dpr;
                    let desiredCount = Math.floor(width / Math.max(1, desiredSpacing));
                    desiredCount = Math.max(8, desiredCount);
                    const lineCount = buf.length > 0 ? Math.min(buf.length, desiredCount) : 0;

                    const display = lineCount > 0 ? resampleLinear(buf, lineCount) : [];

                    const targetMaxValue = display.length > 0 ? Math.max.apply(Math, display) : 0;
                    maxValue = Math.max(targetMaxValue * 0.1 + maxValue * 0.9, 100);

                    const len = Math.max(1, display.length);
                    const barWidth = width / len;

                    ctx.strokeStyle = "white";
                    ctx.lineWidth = 4 * dpr;
                    ctx.lineCap = "round";
                    ctx.lineJoin = "round";

                    const barBeginY = height - barWidth;

                    for (let i = 0; i < display.length; i++) {
                        const x = barWidth * (i + 0.5);
                        ctx.moveTo(x, barBeginY);
                        const norm = Math.min(1, Math.max(0, display[i] / maxValue));
                        ctx.lineTo(
                            x,
                            barBeginY - norm ** 2 * (height - barWidth * 2),
                        );
                    }

                    ctx.stroke();
                    requestAnimationFrame(onFrame);
                }

                onFrame();

                return () => {
                    obs.disconnect();
                    stopped = true;
                };
            }
        }
    }, []);

    return <canvas ref={canvasRef} {...props} />;
};
```

**可视化特性：**
- 使用 `requestAnimationFrame` 实现流畅动画
- 数据平滑处理（指数加权移动平均）
- 自适应条形数量（根据画布宽度）
- 动态调整最大值（自适应增益）
- 支持高 DPI 屏幕
- 响应式布局（ResizeObserver）

### 11.9 线性插值算法

```rust
fn vec_interp(src: &[f32], dst: &mut [f32]) {
    if src.is_empty() {
        dst.fill(0.0);
        return;
    }
    if dst.is_empty() {
        return;
    }
    if src.len() == dst.len() {
        dst.copy_from_slice(src);
        return;
    }
    let src_len = src.len();
    let dst_len = dst.len();
    let src_step = src_len as f32 / dst_len as f32;
    let mut src_idx = 0.0;
    for dst in dst.iter_mut() {
        let src_idx_int = src_idx as usize;
        let src_idx_frac = src_idx - src_idx_int as f32;
        let src_idx_next = src_idx + src_step;
        let src_idx_next_int = src_idx_next as usize;
        let src_idx_next_frac = src_idx_next - src_idx_next_int as f32;
        let src_idx_next_frac_inv = 1.0 - src_idx_next_frac;
        let src_idx_frac_inv = 1.0 - src_idx_frac;
        let v = if src_idx_next_int < src_len {
            src[src_idx_int] * src_idx_frac_inv * src_idx_next_frac_inv
                + src[src_idx_next_int] * src_idx_frac * src_idx_next_frac_inv
        } else {
            src[src_idx_int] * src_idx_frac_inv
        };
        *dst = v;
        src_idx += src_step;
    }
}
```

**插值算法特点：**
- 双线性插值，保证平滑过渡
- 支持上采样和下采样
- 边界处理防止越界访问

### 11.10 性能优化策略

1. **WebAssembly 加速**
   - FFT 计算在 Rust/WASM 中执行，比 JavaScript 快 10-100 倍
   - 零拷贝数据传输（通过 TypedArray）

2. **采样队列管理**
   - 动态调整队列长度（根据实际消耗时间）
   - 限制最大队列长度（2048 * 4 = 8192 样本）

3. **渲染优化**
   - 使用 `requestAnimationFrame` 同步屏幕刷新率
   - Canvas 2D API 硬件加速
   - 避免不必要的重绘

4. **内存管理**
   - 重用缓冲区，减少 GC 压力
   - 及时释放 WASM 实例

### 11.11 配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| FFT 大小 | 2048 | FFT 窗口大小，影响频率分辨率 |
| 采样率 | 44100 Hz | 目标采样率 |
| 频率范围 | 80-2000 Hz | 可视化频率范围 |
| 输出缓冲区 | 64 | 可视化条形数量 |
| 队列最大长度 | 8192 | PCM 队列最大样本数 |

### 11.12 使用示例

```typescript
import { FFTPlayer } from "@applemusic-like-lyrics/fft";

// 初始化 FFT 播放器
const fftPlayer = new FFTPlayer();
fftPlayer.setFreqRange(80, 2000);

// 输入音频数据（从 AudioContext 获取）
const audioBuffer = await audioContext.decodeAudioData(arrayBuffer);
const channelData = audioBuffer.getChannelData(0);
fftPlayer.pushDataF32(audioBuffer.sampleRate, 1, channelData);

// 读取 FFT 结果
const result = new Float32Array(64);
if (fftPlayer.read(result)) {
    console.log("FFT 数据:", result);
}

// 清理资源
fftPlayer.free();
```

---

## 十二、故障排查

### 12.1 常见问题

**问题：无法访问远程控制界面**
- 检查 HTTP 服务器是否启用
- 检查端口 13533 是否被占用
- 检查防火墙设置

**问题：BetterNCM 插件无法连接**
- 检查 AMLL 是否启动
- 检查端口配置是否正确
- 查看插件控制台日志

**问题：WebSocket 连接频繁断开**
- 检查网络稳定性
- 调整心跳间隔
- 检查服务器日志

**问题：FFT 可视化无响应**
- 检查音频数据是否正常输入
- 检查频率范围设置
- 查看浏览器控制台错误

**问题：FFT 性能问题**
- 减少 FFT 大小（2048 → 1024）
- 降低输出缓冲区大小（64 → 32）
- 检查 WASM 模块是否正确加载

---

## 总结

AMLL 远程网页控制系统通过 WebSocket + HTTP REST API 的混合架构，实现了完整的远程控制功能。系统具备以下特点：

1. **高可靠性**：自动重连、错误处理、连接状态监控
2. **高性能**：异步 I/O、连接池、广播机制
3. **易扩展**：模块化设计、协议版本控制、插件系统
4. **跨平台**：支持桌面、移动、浏览器多端访问
5. **现代化**：玻璃拟态 UI、响应式设计、主题切换
6. **实时音频分析**：WebAssembly 加速 FFT、高质量可视化、自适应渲染

该系统为用户提供了便捷的远程控制体验，同时为开发者提供了良好的扩展接口。
